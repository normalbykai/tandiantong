# V1 弃用与迁移说明

## 弃用结论

V1 文档于 2026-07-14 正式冻结。冻结后不再向 V1 阶段目录追加需求、设计、实现计划或验收结果，也不再使用其中关于 JWT、`JdbcTemplate`、旧分支和旧测试状态的描述指导开发。

## 迁移去向

仍然有效的模块化单体、多租户隔离、交易安全、业务范围和测试原则已经整理到 `docs/versions/v1.1/`。V1.1 是后续开发唯一版本依据。

V1 原始文档和原型仍保留，用于追溯需求演进和历史决策，不应删除、改写或复制为新的实施结论。
